Options -Indexes
SetEnvIf Request_URI "^/(proxy|session)\.php$" isolated_endpoint
SetEnvIf Request_URI "^/proxy\.php$" dontlog

RewriteEngine On
RewriteCond %{HTTP_HOST} ^dualviewurl-view\.jessysystem\.com$ [NC]
RewriteCond %{REQUEST_URI} !^/(proxy|session)\.php$ [NC]
RewriteRule ^ - [R=404,L]

<IfModule mod_headers.c>
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-Frame-Options "SAMEORIGIN" env=!isolated_endpoint
    Header always unset X-Frame-Options env=isolated_endpoint
    Header always set Referrer-Policy "no-referrer"
    Header always set Permissions-Policy "camera=(), microphone=(), geolocation=(), payment=(), usb=()"
    Header always set Cross-Origin-Opener-Policy "same-origin"
</IfModule>

<FilesMatch "^\.">
    Require all denied
</FilesMatch>
